diff --git a/components/inputTask.brs b/components/inputTask.brs
index 0c5c51f..f7b9578 100644
--- a/components/inputTask.brs
+++ b/components/inputTask.brs
@@ -35,6 +35,13 @@ function ListenInput()
                     videoFormat: check("", inputData.videoFormat)
                     videoSubs: check("", inputData.videoSubs)
                     startSeconds: strToInt(inputData.startSeconds)
+                    subtitlesEnabled: strToBool(inputData.subtitlesEnabled)
+                    subtitleTracks: check("", inputData.subtitleTracks)
+                    subtitleRequest: check("", inputData.subtitleRequest)
+                    subtitleId: check("", inputData.subtitleId)
+                    subtitleName: check("", inputData.subtitleName)
+                    subtitleUrl: check("", inputData.subtitleUrl)
+                    subtitleReportUrl: check("", inputData.subtitleReportUrl)
                     positionSeconds: strToInt(inputData.positionSeconds)
                     stayPaused: strToBool(inputData.stayPaused)
                     timeOffset: strToInt(inputData.timeOffset)
diff --git a/components/mediaplayer.brs b/components/mediaplayer.brs
index 8590930..73e9211 100644
--- a/components/mediaplayer.brs
+++ b/components/mediaplayer.brs
@@ -29,6 +29,13 @@ sub init()
 
     m.video.observeField("state", "controlvideoplay")
     m.video.observeField("position", "trackvideoprogress")
+    m.video.observeField("availableSubtitleTracks", "applySubtitles")
+    m.video.observeField("currentSubtitleTrack", "reportSubtitles")
+    m.video.observeField("globalCaptionMode", "reportSubtitles")
+    m.subtitleTimer = CreateObject("roSGNode", "Timer")
+    m.subtitleTimer.duration = 0.25
+    m.subtitleTimer.repeat = true
+    m.subtitleTimer.observeField("fire", "pollSubtitles")
 
     m.top.SignalBeacon("AppLaunchComplete")
 
@@ -107,6 +114,8 @@ sub handleInputEvent(msg)
                         handleDeepLink(deeplink)
                     end if
                 end if
+            elseif (deeplink.a = "subtitles")
+                configureSubtitles(deeplink)
             elseif (deeplink.a = "seek")
                 seekVideo(deeplink)
             elseif (deeplink.a = "s")
@@ -122,6 +131,71 @@ sub handleInputEvent(msg)
 end sub
 
 ' Roomcast timestamp protocol. Does not restart or replace the current content.
+sub configureSubtitles(deeplink)
+    m.subtitleTimer.control = "stop"
+    m.subtitleConfig = deeplink
+    if m.subtitleConfig.subtitlesEnabled = invalid
+        m.subtitleConfig.subtitlesEnabled = true
+    end if
+    m.lastSubtitleReport = ""
+    m.subtitlePending = true
+    m.subtitleReportSequence = 0
+    m.subtitlePolls = 32
+    if m.subtitleReporter <> invalid
+        m.subtitleReporter.control = "STOP"
+    end if
+    applySubtitles()
+    if m.subtitlePending then m.subtitleTimer.control = "start"
+end sub
+
+sub pollSubtitles()
+    m.subtitlePolls--
+    applySubtitles()
+    if m.subtitlePolls <= 0 or m.subtitlePending <> true
+        m.subtitlePending = false
+        m.subtitleTimer.control = "stop"
+    end if
+end sub
+
+sub applySubtitles()
+    if m.subtitleConfig = invalid or m.applyingSubtitles = true then return
+    if m.subtitlePending <> true
+        reportSubtitles()
+        return
+    end if
+    m.applyingSubtitles = true
+    if m.subtitleConfig.subtitlesEnabled
+        m.video.globalCaptionMode = "On"
+        trackName = roomcastSubtitleTrack(m.video.availableSubtitleTracks, m.subtitleConfig)
+        if trackName <> "" and m.video.subtitleTrack <> trackName
+            m.video.subtitleTrack = trackName
+        end if
+    else
+        m.video.globalCaptionMode = "Off"
+    end if
+    m.applyingSubtitles = false
+    reportSubtitles()
+end sub
+
+sub reportSubtitles()
+    if m.subtitleConfig = invalid or m.applyingSubtitles = true then return
+    if m.subtitleConfig.subtitleReportUrl = "" or m.subtitleConfig.subtitleRequest = "" then return
+    report = roomcastSubtitleState(m.video, m.subtitleConfig)
+    if report.applied
+        m.subtitlePending = false
+        m.subtitleTimer.control = "stop"
+    end if
+    signature = FormatJson(report)
+    if signature = m.lastSubtitleReport then return
+    m.lastSubtitleReport = signature
+    m.subtitleReportSequence++
+    report.sequence = m.subtitleReportSequence
+    m.subtitleReporter = CreateObject("roSGNode", "subtitleReport")
+    m.subtitleReporter.url = m.subtitleConfig.subtitleReportUrl
+    m.subtitleReporter.report = report
+    m.subtitleReporter.control = "RUN"
+end sub
+
 sub seekVideo(deeplink)
     if m.parsedDeeplink = invalid then return
     if m.parsedDeeplink.t <> "v" then return
@@ -159,6 +233,8 @@ sub updateMetadata(deeplink)
 end sub
 
 sub playaudio()
+    m.subtitleTimer.control = "stop"
+    m.subtitleConfig = invalid
     audiocontent = createObject("RoSGNode", "ContentNode")
     
     audiocontent.url = m.parsedDeeplink.u
@@ -212,6 +288,8 @@ sub SetAlbumName(albumName As String)
 end sub
 
 sub playvideo()
+    m.subtitleTimer.control = "stop"
+    m.subtitleConfig = invalid
     videocontent = createObject("RoSGNode", "ContentNode")
 
     videocontent.title = m.parsedDeeplink.videoName
@@ -223,15 +301,22 @@ sub playvideo()
         end if
     end if
 
-    videocontent.subtitletracks = [{
-        Language: "en-US",
-        Description: "Subtitles",
-        TrackName: m.parsedDeeplink.videoSubs
-    }]
+    tracks = invalid
+    if m.parsedDeeplink.subtitleTracks <> ""
+        tracks = ParseJson(m.parsedDeeplink.subtitleTracks, "i")
+    end if
+    if type(tracks) = "roArray"
+        videocontent.subtitleTracks = tracks
+    elseif m.parsedDeeplink.videoSubs <> ""
+        videocontent.subtitleTracks = [{
+            Language: "eng", Description: "Subtitles", TrackName: m.parsedDeeplink.videoSubs
+        }]
+    end if
 
     m.video.seekMode = "accurate"
     m.video.autoplayAfterSeek = true
     m.video.content = videocontent
+    configureSubtitles(m.parsedDeeplink)
 
     m.video.enableTrickPlay = true
     m.video.trickPlayBar.filledBarBlendColor = "#662d91"
diff --git a/components/mediaplayer.xml b/components/mediaplayer.xml
index 99d6eca..8f4d9fb 100644
--- a/components/mediaplayer.xml
+++ b/components/mediaplayer.xml
@@ -8,6 +8,7 @@
     </interface>
 
         <script type="text/brightscript" uri="pkg:/components/mediaplayer.brs" />
+        <script type="text/brightscript" uri="pkg:/components/subtitleState.brs" />
 
         <children>
 
diff --git a/manifest b/manifest
index 324f4f2..200476c 100644
--- a/manifest
+++ b/manifest
@@ -1,7 +1,7 @@
 title=Roomcast Player
 major_version=1
 minor_version=3
-build_version=2
+build_version=5
 
 ##   Channel Assets
 ###  Main Menu Icons / Channel Poster Artwork 
diff --git a/source/main.brs b/source/main.brs
index 4f21302..6d1f1dd 100644
--- a/source/main.brs
+++ b/source/main.brs
@@ -43,6 +43,13 @@ Function getDeepLinks(args) as Object
             videoFormat: check("", args.videoFormat)
             videoSubs: check("", args.videoSubs)
             startSeconds: strToInt(args.startSeconds)
+            subtitlesEnabled: strToBool(args.subtitlesEnabled)
+            subtitleTracks: check("", args.subtitleTracks)
+            subtitleRequest: check("", args.subtitleRequest)
+            subtitleId: check("", args.subtitleId)
+            subtitleName: check("", args.subtitleName)
+            subtitleUrl: check("", args.subtitleUrl)
+            subtitleReportUrl: check("", args.subtitleReportUrl)
             timeOffset: strToInt(args.timeOffset)
             duration: strToInt(args.duration)
             enqueue: strToBool(args.enqueue)
